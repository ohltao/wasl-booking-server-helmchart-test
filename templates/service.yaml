apiVersion: v1
kind: Service
metadata:
  name: {{ include "booking-helm-test.fullname" . }}
  labels:
    {{- include "booking-helm-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "booking-helm-test.selectorLabels" . | nindent 4 }}
